import React from 'react';

const ButtonSet = () => {
    return (
        <div>
            <button>Thêm Mới</button>
            <button>Cập Nhật</button>
            <button>Xóa</button>
        </div>
    );
};

export default ButtonSet;
